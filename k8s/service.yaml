apiVersion: v1
kind: Service
metadata:
  name: webhook-admission-mutation
  labels:
    app: webhook-admission-mutation
    component: mutating-webhook
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: webhook-admission-mutation
  selector:
    app: webhook-admission-mutation
